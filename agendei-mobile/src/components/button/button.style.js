import { COLORS, FONT_SIZE } from "../../constants/theme.js"

export const styles = {
    
    btn: {
        with: "100%",
        backgroundColor: COLORS.blue,
        padding: 6,
        borderRadius: 12
    },
    text: {
        color: "#FFF",
        fontSize: FONT_SIZE.md,
        textAlign: "center"
    }
}